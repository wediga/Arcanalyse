[project]
name = "arcanalyse-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wediga" }
]
requires-python = ">=3.12"
dependencies = [
    "arcanalyse-core",
    "psycopg[binary]>=3.2",
]

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
arcanalyse-core = { workspace = true }